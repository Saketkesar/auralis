"""Pure utility functions shared across AURALIS VISION engines.

This package holds small, dependency-free helpers (scoring, ranking,
threshold flags, format detection) that engines compose. They are kept pure so
they can be exhaustively property-tested in isolation.
"""
