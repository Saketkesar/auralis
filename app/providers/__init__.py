"""Pluggable search providers package.

Holds the provider base interface and the built-in general/image/map/social
providers. Providers self-register against the Search Hub so new providers
become available without changes to existing ones.
"""
